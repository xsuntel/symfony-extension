# Memory — tools-vscode-extension-author

Hand-maintained notes, read explicitly by
[`.claude/agents/tools-vscode-extension-author.md`](../../agents/tools-vscode-extension-author.md).
The live source is the final authority when a note conflicts with it.

## Scaffolding conventions confirmed here

_No entries yet._ Record a scaffolding decision once it has been settled and would otherwise be
re-derived: a provider skeleton shape, a `contributes` block the reviewer accepted, or a manifest
field this project sets differently from the VSCode default.

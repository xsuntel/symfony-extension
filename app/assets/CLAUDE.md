# CLAUDE.md

## File Structure

```text
symfony-extension/                           ← Repository root
└── app/                                     ← Symfony Application Root
    └── assets/
        └── images/
            ├── symfony.svg                  ← Activity bar icon (vector, preferred)
            └── symfony.png                  ← Activity bar icon (raster fallback)
```
